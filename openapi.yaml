# AUTO-GENERATED from api.main.app.openapi() -- do not edit by hand.
# Regenerate with: <venv>/bin/python -c "import yaml,api.main; print(yaml.safe_dump(api.main.app.openapi(), sort_keys=False, allow_unicode=True))" > openapi.yaml
openapi: 3.1.0
info:
  title: Decision Intelligence Layer — Core
  version: 1.2.0
paths:
  /v1/metrics:
    post:
      summary: Update Metric
      operationId: update_metric_v1_metrics_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMetricRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateMetricResponseBody'
        '422':
          description: Unprocessable Entity
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/scoreboard/{decision_id}:
    get:
      summary: Get Scoreboard
      operationId: get_scoreboard_v1_scoreboard__decision_id__get
      parameters:
      - name: decision_id
        in: path
        required: true
        schema:
          type: string
          title: Decision Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetScoreboardResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/merkle-proof:
    get:
      summary: Get Merkle Proof
      operationId: get_merkle_proof_v1_merkle_proof_get
      parameters:
      - name: decision_id
        in: query
        required: true
        schema:
          type: string
          title: Decision Id
      - name: metric_name
        in: query
        required: true
        schema:
          type: string
          title: Metric Name
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetMerkleProofResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /healthz:
    get:
      summary: Healthz
      operationId: healthz_healthz_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
components:
  schemas:
    ErrorResponse:
      properties:
        detail:
          type: string
          title: Detail
      type: object
      required:
      - detail
      title: ErrorResponse
    GetMerkleProofResponse:
      properties:
        leaf_hash:
          type: string
          title: Leaf Hash
        proof_path:
          items:
            $ref: '#/components/schemas/ProofNodeOut'
          type: array
          title: Proof Path
        root:
          type: string
          title: Root
        anchor_signature:
          type: string
          title: Anchor Signature
        anchor_timestamp:
          type: string
          format: date-time
          title: Anchor Timestamp
      type: object
      required:
      - leaf_hash
      - proof_path
      - root
      - anchor_signature
      - anchor_timestamp
      title: GetMerkleProofResponse
    GetScoreboardResponse:
      properties:
        decision_id:
          type: string
          title: Decision Id
        trust_score:
          anyOf:
          - type: number
          - type: string
          title: Trust Score
        metrics:
          additionalProperties:
            $ref: '#/components/schemas/ScoreboardMetricOut'
          type: object
          title: Metrics
        merkle_root_ref:
          anyOf:
          - type: string
          - type: 'null'
          title: Merkle Root Ref
        merkle_timestamp:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Merkle Timestamp
        rebuilt_at:
          type: string
          format: date-time
          title: Rebuilt At
      type: object
      required:
      - decision_id
      - trust_score
      - metrics
      - merkle_root_ref
      - merkle_timestamp
      - rebuilt_at
      title: GetScoreboardResponse
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    JsonValue: {}
    ProofNodeOut:
      properties:
        hash:
          type: string
          title: Hash
        position:
          type: string
          enum:
          - LEFT
          - RIGHT
          title: Position
      type: object
      required:
      - hash
      - position
      title: ProofNodeOut
    ScoreboardMetricOut:
      properties:
        value:
          title: Value
        evidence_refs:
          items:
            type: string
          type: array
          title: Evidence Refs
        committed_at:
          type: string
          format: date-time
          title: Committed At
      type: object
      required:
      - value
      - evidence_refs
      - committed_at
      title: ScoreboardMetricOut
    UpdateMetricRequest:
      properties:
        decision_id:
          type: string
          title: Decision Id
        metric_name:
          type: string
          title: Metric Name
        value:
          title: Value
        idempotency_key:
          type: string
          minLength: 16
          title: Idempotency Key
        evidence_refs:
          items:
            type: string
          type: array
          minItems: 1
          title: Evidence Refs
        proposed_by:
          type: string
          title: Proposed By
        runtime_context:
          $ref: '#/components/schemas/JsonValue'
      type: object
      required:
      - decision_id
      - metric_name
      - value
      - idempotency_key
      - evidence_refs
      - proposed_by
      title: UpdateMetricRequest
    UpdateMetricResponseBody:
      properties:
        status:
          type: string
          enum:
          - ACCEPTED
          - DUPLICATE_IGNORED
          title: Status
      type: object
      required:
      - status
      title: UpdateMetricResponseBody
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
        input:
          title: Input
        ctx:
          type: object
          title: Context
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
